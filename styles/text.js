import styled from '@emotion/styled'
import { css } from '@emotion/core'

export const textMenu = css`
    font-family: 'Oswald', sans-serif;
    font-size: 14px;
    line-height: 24px;
    transform: translateY(6px);
    font-weight: 400;
`
